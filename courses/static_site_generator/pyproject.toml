[project]
name = "nghiango-static-site-generator"
version = "0.3.1"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["src"]
